# حالة مشروع كدان — 2026-09-13

## الهدف المعتمد
مراجعة المنصة وإصلاح اللازم، مع الاحتفاظ بسياق قابل للنقل إلى محادثة جديدة عند تعذر استمرار المحادثة الحالية.

## المشروع
- المستودع: https://github.com/kedan-ksa/Planner
- المسار المحلي: C:/Users/user/Documents/Codex/2026-08-20/codex
- التطبيق: https://planner.ahmad-2a4.workers.dev
- Next.js / Prisma / PostgreSQL على Azure، والنشر إلى Cloudflare Workers من GitHub Actions.
- حساب الشركة المطلوب كمدير نظام: ahmad@kedan.com.sa.
- لا تحفظ أو تنقل أسرارًا في هذا الملف أو في المحادثة.

## الحالة عند بدء المراجعة
كانت هناك تعديلات غير ملتزمة في Git، تشمل التقارير والصلاحيات والتحقق من شجرة الإدارات وسلاسل الاعتماد. يجب الحفاظ عليها. آخر نسخة أعلن نشرها في المحادثة السابقة هي 2902140؛ لا تعتبر ذلك تحققًا من حالة الإنتاج الحالية.

## إصلاحات هذه المراجعة
- حماية POST لمزامنة Planner بفحص Origin وSec-Fetch-Site.
- تقييد استعلام مبادرات صفحة المهام بالمؤسسة الحالية.
- تصفية المهام المسندة قبل حد الصفحة، وإضافة السابق/التالي بصفحات 50 مهمة وترتيب ثابت.
- تحديث اختبار إتاحة مركز الموافقات لمدير الإدارة وفق سلسلة الاعتماد.
- إضافة اختبارات محتوى التقارير وقفل التحرير والصلاحيات وفحص مصدر الطلب.
- إظهار المهام المسندة للموظف عبر إدارات المؤسسة نفسها، وضبط حدود ترقيم الصفحات.
- توحيد حالة اكتمال المهمة مع النسبة؛ حفظ المهمة والسجل ومتوسط المبادرة داخل معاملة واحدة مع قفل الصفوف.
- قصر إنشاء سلاسل الاعتماد على التقارير ومنع إنشاء سلسلة نشطة مكررة للإدارة.
- معاينة التقرير للمعتمد المكلّف خارج إدارته داخل المؤسسة فقط، دون توسيع صلاحية التحرير.
- إظهار تنبيه بأن التحديث محلي وأن مزامنة Planner قد تستبدل النسبة، مع بقاء سجل الملاحظات.

## التحقق
- 29 اختبارًا نجح، منها اختبارات حدود معاينة التقارير باستخدام قاعدة بيانات مقلدة؛ ليست اختبارات تكامل PostgreSQL.
- TypeScript نجح بعد إصلاح نوع organizationId.
- نجح البناء النهائي بعد التعديلات في 2026-09-10 بإعداد cpus: 2، ونجح Lint وTypeScript. لا يوجد تحقق من نشر هذه التعديلات إلى الإنتاج بعد.

## النشر والتحقق التشغيلي
- دُمج طلب المراجعة #2 في main بالالتزام da667762b8d8b5cc1f7b9f40944351995b868a02.
- نجح نشر Cloudflare في التشغيل 34480692220 بتاريخ 2026-09-10، بما يشمل فحوصات الكود وخطوة قاعدة البيانات.
- فُتحت صفحة التقارير الجديدة بحساب أحمد محيسن (مدير النظام)، ونجحت إعادة حفظ المسودة بمحتواها الحالي دون تغييره؛ ظهرت رسالة «تم الحفظ بنجاح».
- لم يُرسل تقرير حقيقي للاعتماد للاختبار. لا يوجد تحقق مكتمل لمعاينة التقرير أو حسابات الموظف والمدير.
- في استئناف 2026-09-13 لم يعد اتصال المتصفح Chrome السابق متاحًا.
- إصلاح مستقل لمسار Docker القديم: نقل شرط webhook إلى env وتصحيح اسم صورة الحاوية إلى أحرف صغيرة. سبب الفشل المؤكد كان Invalid workflow file / Unrecognized named-value: secrets.

## نقاط المراجعة المتبقية
- تجربة الحفظ الحقيقي بحساب موظف وحساب مدير ضمن نطاقيهما.
- اختبار إرسال تقرير ثم إعادته وإعادة إرساله واستكمال سلسلة الاعتماد، بما يشمل الطلبات المتزامنة.
- تحديثات المهام المحلية تعدّل نسبة المهمة محليًا؛ المزامنة اللاحقة من Planner يمكن أن تستبدل النسبة. لا توجد كتابة إلى Planner في هذا المسار.
- الأنواع غير التقارير لا تظهر لإنشاء سلاسل جديدة؛ السجلات السابقة محفوظة ولا تزال غير مدعومة في قرارات الاعتماد.
- مراجعة جميع التعديلات الموجودة مسبقًا قبل الالتزام والنشر؛ نجاح اختبارات الوحدات لا يثبت نجاح معاملات PostgreSQL على Workers.
- نموذج التقرير النهائي سيقدمه المستخدم لاحقًا؛ لا تدّعِ مطابقته قبل استلامه.

## تعليمات استئناف لمحادثة جديدة
اقرأ هذا الملف ثم git status وgit diff. تابع مراجعة العمل الحالي دون إعادة إنشائه أو حذف تعديلاته. افصل في التقرير بين ما اختُبر محليًا وما نُشر وما جُرّب فعليًا. لا تنقل المفاتيح أو كلمات المرور السابقة.
